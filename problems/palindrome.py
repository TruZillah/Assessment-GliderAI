"""
Problem 2: is_palindrome(s) -> bool
Return True if s is a palindrome ignoring non-alphanumeric characters and case.

Examples:
- is_palindrome("A man, a plan, a canal: Panama") -> True
- is_palindrome("race a car") -> False
- is_palindrome("") -> True

Notes:
- Consider only letters and digits (use str.isalnum()).
"""

# TODO: Implement the function below.

def is_palindrome(s):
    """Check if s is a palindrome ignoring non-alphanumeric and case."""
    # Write your code here
    raise NotImplementedError("Implement two-pointer check after filtering")
