"""
Two Sum II (Input array is sorted)
Implement two_sum_sorted(nums, target) -> List[int] returning 0-based indices or [-1,-1].
"""

def two_sum_sorted(nums, target):
    """Two-pointer approach on sorted input."""
    raise NotImplementedError("Use left/right pointers moving inward based on sum")
